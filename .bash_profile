#!/bin/bash
# ~/.bash_profile - Sourced for login shells
# Sources ~/.bashrc for consistent configuration

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

# Login-shell specific settings below
# (e.g., start X, set display variables, etc.)
